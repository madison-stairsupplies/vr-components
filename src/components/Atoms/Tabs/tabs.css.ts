import { recipe } from "@vanilla-extract/recipes";

export const tabContainer = recipe({
    base: [{ display: "flex" }],
    variants: {},
});

export const tabItem = recipe({
    base: [],

    variants: {},
});
export const tabBody = recipe({
    base: [],

    variants: {},
});
