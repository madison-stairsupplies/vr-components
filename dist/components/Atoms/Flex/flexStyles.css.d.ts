export declare const gapRecipe: import("@vanilla-extract/recipes").RuntimeFn<{
    container: {
        xs: {
            width: string;
            margin: `-${number}px 0 0 -${number}px`;
        };
        sm: {
            width: string;
            margin: `-${number}px 0 0 -${number}px`;
        };
        md: {
            width: string;
            margin: `-${number}px 0 0 -${number}px`;
        };
        lg: {
            width: string;
            margin: `-${number}px 0 0 -${number}px`;
        };
        xl: {
            width: string;
            margin: `-${number}px 0 0 -${number}px`;
        };
        xxl: {
            width: string;
            margin: `-${number}px 0 0 -${number}px`;
        };
    };
    item: {
        xs: {
            padding: `${number}px 0 0 ${number}px`;
        };
        sm: {
            padding: `${number}px 0 0 ${number}px`;
        };
        md: {
            padding: `${number}px 0 0 ${number}px`;
        };
        lg: {
            padding: `${number}px 0 0 ${number}px`;
        };
        xl: {
            padding: `${number}px 0 0 ${number}px`;
        };
        xxl: {
            padding: `${number}px 0 0 ${number}px`;
        };
    };
}>;
